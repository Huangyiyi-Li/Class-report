# v0.2.0-codex.33

Windows 10/11 x64 内部测试版，优先下载 Setup 安装版。

## 本次修复

- 设备认证成功但学校、类型或教室字段不完整时，保留已有本地绑定，避免上传重试将绑定信息写空，重启后显示“尚未绑定教室”。
- 返回完整归属时仍按既有逻辑整体刷新；不拼接新旧学校身份。
- 诊断新增 bindingRefresh，记录是否跳过归属刷新以及缺失的字段名，便于核对接口返回。
- 保留 .32 的新首页域名识别、Passport 自动关窗和错误码传递修复。

## 安装后检查

1. 如果原绑定已被旧版本写空，升级后需重新绑定一次；本版本无法从设备编号推断恢复学校和教室。
2. 完成绑定和录音后，点击待上传录音的重试，再关闭并重新打开客户端，确认教室绑定仍保留。
3. 如仍异常，请导出诊断，重点核对 bindingRefresh、authIssue 和 binding。

## 验证边界

本地 Python 230 项通过、1 项跳过，Node 206 项通过，界面构建通过。Windows CI 执行自动化测试、Electron 回归、安装包构建和正常启动检查，结果见本标签对应 Actions。真实学校账号、接口返回及录音现场验收仍需复测。

此前 OSS 凭证问题由开发处理，不作为本次修复内容。
